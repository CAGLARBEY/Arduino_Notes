#define LED_PIN 13

void setup() {
  pinMode(LED_PIN, OUTPUT);        // Pin çıkış olarak ayarlanır
  digitalWrite(LED_PIN, HIGH);     // LED yakılır
}

void loop() {
  // Bir şey yapmıyoruz, LED hep yanıyor
}
